# 统一换行符策略
#
# 背景（CI 血泪教训）：gradlew 曾被以 CRLF 提交入库，Linux runner 上会报
#   ./gradlew: /bin/sh^M: bad interpreter: No such file or directory
# 且 chmod +x 无法修复（问题在行尾，不在权限）。因此这里显式强制：
#   - 所有文本默认按 LF 存储（仓库内统一 LF）；
#   - shell 脚本与 gradlew 一律 LF 检出（Linux/macOS 可直接执行）；
#   - .bat / .cmd 一律 CRLF 检出（Windows 批处理要求 CRLF）。
* text=auto

# 仓库内统一 LF
*.kt   text eol=lf
*.kts  text eol=lf
*.java text eol=lf
*.xml  text eol=lf
*.md   text eol=lf
*.json text eol=lf
*.yml  text eol=lf
*.yaml text eol=lf
*.toml text eol=lf
*.properties text eol=lf

# Shell 脚本必须 LF，否则 Linux 无法执行
gradlew   text eol=lf
*.sh      text eol=lf

# Windows 批处理保持 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制文件不做任何转换
*.jar  binary
*.apk  binary
*.png  binary
*.jpg  binary
*.jpeg binary
*.webp binary
*.zip  binary
*.doc  binary
*.docx binary
